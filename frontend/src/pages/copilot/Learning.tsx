import { GraduationCap } from 'lucide-react';
import CopilotPlaceholder from './CopilotPlaceholder';

export default function Learning() {
  return (
    <CopilotPlaceholder
      icon={GraduationCap}
      title="Learning"
      subtitle="Answer Bank, profiles, and bias"
      note="Learning ships with PH7."
    />
  );
}
